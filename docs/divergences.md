# Divergences and chosen readings

Every place this project had to choose a reading of IDTA 02004, IDTA
02003, IDTA 02035-2, or of the official material, with the evidence. Non-empty on day one: the official
template (2.0.1) and the official example disagree with each other in
the ways recorded below, so a validator has to decide whom to believe
before it can report anything.

| # | observation (verified against the published files, or against the requirements indexes in `data/battery-passport/` where a row says so) | our reading |
|---|---|---|
| 1 | The official example's `Entities` list carries idShort `Entites` (sic). | idShort is not a matching key (Annex A: "A different idShort might be chosen"), so a typo there is not a finding. Matching is by semanticId throughout. |
| 2 | The example's Entities list uses semanticId `…/EntityForDocumentation` (the *child* element's singular IRI) where the template says `…/EntitiesForDocumentation`. | Reported as a template-conformance finding with a nearest-miss diagnosis, not silently matched. **And what that costs, which this entry did not say until 0.1.2**: the list matches no row, so the walk does not enter it, and `HD-E38` -- `EntityForDocumentation`, which the template makes mandatory inside that list, one or more -- is never put. The example passes on ten findings, none of them an error, with one rule unasked. `summary.rulesNotAsked` says so now; before it, this project's own reference material was a silent demonstration of #23 and nothing in the report showed it. |
| 3 | The template's language list is idShort `Language`; the example says `Languages`. | Harmless under semanticId matching; recorded so nobody "fixes" matching to depend on names. |
| 4 | Only `OrganizationShortName`'s main semanticId is an ECLASS-CDP URL (`https://api.eclass-cdp.com/0173-1-02-ABI002-003`) where every sibling uses an IRDI — in the template *and* the PDF alike. | semanticId comparison normalises CDP URLs to IRDIs, both directions; an instance carrying either spelling matches. |
| 5 | The template's `AllowedIdShort` qualifier values are written like `RefersTo[\d{2,3}]` — as a regular expression this is a character class and matches "RefersTo" plus one character. | We implement the evident intent, `^RefersTo(?:\d{2,3})?$` — the digits are the multiple-instance suffix and so optional, which is why the template's own single `PreviewFile` passes. Always an informational lint (HDL1). |
| 6 | The official template 2.0.1 itself fails aas-core3.0 metamodel verification — four errors across three conceptDescriptions: `ClassificationSystem` (0173-1#02-ABH997#003) carries both a duplicate-language `displayName` and a duplicate-language `definition`; `DocumentVersion` carries the duplicate `definition` alone; `PreviewFile` (0173-1#02-ABK127#002) the duplicate `displayName` alone. | The `meta` channel reports what the verifier finds, on official material as on anyone's. |
| 7 | The short name `RefersTo` appears in the PDF's §2.2 prose; all three short names (`RefersTo` / `BasedOn` / `TranslationOf`) appear in the template's `AllowedIdShort` qualifiers. The PDF's *tables* and the template's SML idShorts use the longer `RefersToEntities` / `BasedOnReferences` / `TranslationOfEntities`. | Harmless under semanticId matching; recorded so the short forms are not mistaken for element identities. |
| 8 | The template writes a list item's identity as two ECLASS ids joined by "/" inside one key (`0173-1#02-ABI500#003/0173-1#01-AHF579#003`), plus `~0`-suffixed and CDP-URL supplemental spellings. | A row matches on the union of the whole composite, its `~N`-stripped form, and the CDP-normalised form — **not** its "/"-split components. An earlier reading split composites into components ("the PDF cites the item id alone"); checked against the PDF that claim is false (the tables carry the composite), and the splitting let a Document collection's id match its parent Documents-list row. Composites are kept whole. |
| 9 | §2.3 says the mandatory classification is "identified by the value 'VDI 2770 Blatt 1:2020'" — but the template's own ExampleValue, and therefore the official example, write `VDI2770:2020`. | Both spellings count as the mandatory system (HD-D2..D4 apply to either); the non-canonical spelling draws HDL5, a warning that names §2.3's string. Failing the official example on the template's own example spelling would be dogma; matching only the example spelling would betray the specification's sentence. |
| 10 | The official example addresses list children by index in ModelReferences ("Documents / 0 / ...") while giving those children (illegal) idShorts. | Reference resolution accepts the index inside a SubmodelElementList regardless of any idShort present: the idShort is the AASd-120 violation, the reference is correct. |
| 11 | The official example's *leaf-type* list children (Reference/Property/File/Entity) carry no main semanticId of their own; its collection-type list children (Document, DocumentId, …) do carry one. | Inside a list scope, the sole child row also matches by element kind when the child has no *main* semanticId — even if it has a supplemental one (the example's language codes do). Keying the fallback on the full candidate set instead broke exactly those language children. |
| 12 | The template's own submodel `semanticId` is a `ModelReference` with a `Submodel` key, not an `ExternalReference`. | The expected reference type is read from the template, so an instance mirroring it (ModelReference) is conformant. An `ExternalReference` with the same key value identifies the same template and draws HDL3 (info), not a failure. An earlier version hardcoded `ExternalReference` as expected and so reported a false HDL3 against the official example. |
| 13 | ECMA-376 (OPC) resolves a relationship `Target` that does not begin with "/" against the *source part's* directory, not the package root. | Relationship targets are resolved that way (`X2`/`X4`); treating every target as root-relative rejected conformant packages that use relative targets. Absolute targets (leading "/") are unchanged. |
| 14 | An element may declare its identity through `supplementalSemanticId`s as well as its main `semanticId`. | Matching folds an element's supplementals into its candidate spellings, symmetrically with the template side. The in-list kind fallback still keys on the *main* semanticId being absent, so a child identified only by a supplemental is not mistaken for a mislabelled one. |
| 15 | §2.1: VDI 2770 requires PDF/A files. | HD-D10 checks for a DigitalFile of content type `application/pdf` per DocumentVersion, as a **warning**: a content type establishes PDF, not PDF/A conformance, and a non-PDF rendition (e.g. a CAD model) is a real-world case §2.1 flags but that this tool cannot call an error. |
| 16 | An element that declares its main identity as one row's semanticId *and* a sibling row's identifier as a `supplementalSemanticId` claims two rows' identities at once. | The walk assigns it to one row (first match wins), but the hand-rule navigation (`child_of`) matches on the union and may also read it for the other row, so such an element is **over-reported**. It is not hidden and no conformant or official-example element does this (real supplementals are same-identity CDP/`~N` spellings) — a mis-modelled element only. Left as a known limitation rather than risk a navigation refactor. |
| 17 | `xs:date`'s lexical space allows years outside `datetime.date`'s 1–9999 range; HD-D8 and TD-D1 check the value lexically to accept them (e.g. `-0001-01-01`). Three far-edge spellings used to diverge from aas-core3's reading and were recorded here rather than chased: a `0000` or leading-zero year was over-accepted, and a BCE leap year was judged in both directions. | Chased, because the ledger was the wrong place for them. Two of the three were **over-rejections** — `-0001-02-29` drew a MUST finding against a file aas-core3 calls conformant — and the metamodel channel is no backstop in that direction: it accepts what it accepts and says nothing. XSD 1.0 has no year zero and no leading zero past four digits, and `-0001` is 1 BCE, so the leap rule divides a year one greater. All three are read the way aas-core3 reads them now; `tests/test_values.py` compares the two readings over the whole corpus and names what is left (#31). |
| 18 | A `File` element's value and an OPC relationship `Target` are different kinds of string: a Target is a URI reference resolved against its source part (#13), while a File value is a part name, which ECMA-376 Part 2 defines as absolute, "/" separated, reserved characters percent-encoded, and free of empty, `.` or `..` segments. Files in the wild are not written that way. | One normaliser, two entry points. The spelling is normalised for both (the other separator, a leading `./`, a doubled separator, a step up and back, a percent escape); where the name starts from stays different. A literal match is tried before the normalised one, so an archive that really holds `%20` in an entry name keeps it — and relationship resolution lands on the entry the archive holds rather than on the normalised spelling, The two entry points ask in different orders -- the chain tries the literal first, the File rule the normalised first -- so on an archive holding two spellings of one name, or a value that is no part name at all, they can still land on different entries. Four cases were constructed and they do: a value of `../evil.step`, an archive holding both `files/x.step` and `/files/x.step`, a target resolving to a part that is absent while the File rule's normalisation finds one that is present, and an archive entry whose name ends in whitespace with a File value naming it exactly. The fourth is the sharpest and was found last: `part()` returns the entry, and `HD-D7` reports that the container holds no part at that value, because the rule folds the value before it asks and the literal match `part()` tries first therefore never sees the value the archive holds. The reader contradicting itself in one report is worse to read than either answer alone. It is still not a conformant file being refused -- an OPC part name is built from pchar and a literal space is not one, so no conformant package produces it -- which is why this stays recorded rather than repaired in a release already carrying several verdict changes. Nothing published produced any of the four. **Closed in 0.1.2 by making it one entry point rather than two**: the folding moved inside `part()`, after the literal steps instead of before them, so both callers ask in the same order and the fourth case can no longer arise. What is deliberately not folded is the archive's own entry names -- an archive holding `aasx/files/manual.pdf ` against a value of `/aasx/files/manual.pdf` stays refused, since matching it would mean supplying a character the value does not carry, and that row is in the verdict corpus to keep it that way. Measured against 0.1.1: one verdict moves, the fourth case, from `HD-D7` and exit 1 to silence and exit 0. **The grammar itself is enforced from 0.1.2 as well**: this row's own first sentence says a part name percent-encodes its reserved characters, and nothing checked, so `x?y.pdf` and `a<b>.pdf` were reported as parts the container does not hold -- sending an author to add a file that no entry can be named after -- and `[Content_Types].xml`, which OPC does not make a part at all, drew nothing. The check names the offending character and is asked only of a value a document wrote: an archive entry name is not a part name, and applying the grammar to the archive's own names took oddly spelled entries out of the canonical index and made `X4` report a part missing that was in the package. Two conservative exceptions, recorded rather than enforced: a lone `%` and any character outside ASCII. A target that is no part name at all is passed through unresolved rather than dropped: dropping it took X4's question away with it. Evidence for the strict reading: of the ten File elements in the official 02004 example, the seven that name a part all name it absolutely (two carry an empty value and one an external URL, neither of which is a part name); 02003's sample names three, also absolutely, and the official packager sidesteps escaping by substituting characters (`https___demo_com_ContactInformationAAS`) rather than encoding them. A value that is not a part name at all -- it climbs out of the package, names a directory, or carries a character no segment may hold -- is a distinct finding from a part being absent, and HD-D7 says so. **They stopped sharing a remedy in 0.1.2** and this row used to give that as the reason they are one rule: the not-a-part-name branch now carries its own, because the rule's own sentence says to add the file under the name the value gives and no entry added under `x?y.pdf` is a part. What keeps them one rule is the thing a consumer gates on -- in both, the document points at bytes this reader cannot retrieve. Whether that is still the right split is an open question and not one to settle as a side effect of improving a remedy: it would move a published rule id and the count on the front page. **0.1.4 adds the one folding this row did not have.** ECMA-376 Part 2 (5th edition, December 2021) 6.2.2.3 decides part name equivalence by ASCII case-insensitive matching — spelling the mapping out, 0x41-0x5A compared as 0x61-0x7A — and its Example 1 says a package holding `/a` cannot also hold `/A`; 7.2.5.5 maps a ZIP item to a part name with an *equivalent* prefix rather than an identical one. So `aasx/files/Manual.pdf` and a value of `/aasx/files/manual.pdf` are one name, and reporting the part absent sent an author to add a file already in the package. This is not the whitespace case above, which stays refused for the reason given there: matching that would mean supplying a character the value does not carry, while case equivalence is the standard's own statement about what two names mean. Same family as #34 and #35, which fold a boolean's case and a language tag's on the argument this row can now borrow — a finding on a conformant file is the direction with no second opinion. ASCII only: the same subclause keeps NFC/NFD collisions under *should not*, as advice to whoever writes a package, so `É` and `é` stay two names and `str.lower()` would have answered that a part is present which the archive does not hold. Asked last, after every exact spelling, so an archive holding the case the document wrote still answers with that entry; an archive holding two entries differing only in case is already outside 6.2.2.3 and resolves in the archive's own write order, pinned across twelve hash seeds. No corpus verdict moves. **The review that raised this cited "M1.12" from memory; that identifier does not appear in the 5th edition** — the M-numbered rules belong to earlier ones — **and the clause that carries the requirement is 6.2.2.3.** Right substance, wrong anchor, which is why the reading was held until the document was opened. |
| 19 | 02003 §3.5: "the set of suitable semanticIds is not restricted". Thirty-six elements of the template are placeholders for content a manufacturer may add, all of which share one identifier, so six siblings in a single scope carry the same one. | No rule is generated from them, and the generator drops the subtree before it is numbered. Generating rules would demand the unconstrained; and with six siblings sharing an identifier, the first row would claim every arbitrary element the walk met and then fault it for being the wrong kind. A manufacturer's own properties pass without comment, which is what the sentence says. |
| 20 | Four of 02003's list items carry no `SMT/Cardinality` qualifier at all, where 02004 qualified every element. | Absent means 0..*, which is what the PDF's element tables (3, 5, 7 and 11) give each of them. Failing would refuse the published template; assuming One would invent an obligation the standard does not state. Those rows cannot be violated by count, so the suite proves them live by kind instead. |
| 21 | 02003 gives ClassificationSystem four `SMT/ExampleValue/*` qualifiers — one per classification system — where 02004 used a single bare `ExampleValue`. | All four are joined into the remedy, in template order. Keeping the first would tell a reader ECLASS is the answer where the template offers four. Annex B (Issue #190) prescribes exactly this — "Example values are now separated by '|' characters" — which was found after the fact rather than before; the same issue claims the qualifiers' semanticId was corrected to `…/ExampleValue/1/0`, and all four in the published 2.0.1 file still read `…/AllowedValue/1/0`. |
| 22 | 02003's `ClassificationSystemUrl` carries the identifier `…/ProduktClassification/…` — German spelling inside an otherwise English IRI. | Matched verbatim. An identifier is opaque; correcting it would match nothing the published template declares. Recorded because an instance author who "fixes" the spelling gets **silence**: the near-miss lint compares the last path segment of an IRI, and this typo is in a middle one, so the corrected identifier matches no row and is diagnosed by nothing. That is consistent with the matching policy — an unknown identifier is allowed, the template states a minimum — but it is not the lint an earlier version of this row claimed. Measured, not assumed. |
| 23 | A list item bounded 0..* cannot be violated by count, so an item whose identifier is wrong matches no row, is never recursed into, and takes its children's rules out of the run with it. Measured: a ProductClassification item with a drifted identifier and its three mandatory children deleted reports TDL1 alone, where the same deletion under a correct identifier reports TD-E13, TD-E16 and TD-E17. | Known limitation, recorded rather than chased, and wider than the sentence to the left suggests. Measured across all 86 generated rows: a wrong identifier turns **12 rows from exit 1 into exit 0**, and **33 of the 86 report nothing at all**. `tools/scope_silence.py` recomputes a sharper version of the second figure on every run of the suite, and it separates two shapes this sentence ran together. Of the 86 rows, **69 appear in the fixtures at all**; the other 17 cannot be measured this way. Of those 69, a **version bump** -- the last character of the identifier -- leaves **none** of them silent: 18 lose their own rule and every one of the 18 still speaks through the near-miss lint, which is what the lint is for and is the measurement that justifies it. A **typo inside a path segment that is not the last** leaves **18 of the 69 saying nothing whatever**, lint included, which is the case #22 predicts and the one worth closing. (That figure read 24 until the tool's own mutation was checked: it substituted a fixed `9`, so ten identifiers ending `...9#00N` were handed back unchanged and counted as silent. An unmutated fixture is the quietest input there is.) It is not confined to list items and not confined to `0..*`: `FurtherInformation` and `Entities` sit directly under a collection and a submodel, and a `1..*` row loses its subtree whenever one conformant sibling survives -- which covers the four most expensive rows in the tree, `Document` and `DocumentVersion` in both packs. The near-miss lint catches a version drift because that is what it looks for; a typo in a middle path segment defeats both (see #22), and then the subtree is silent entirely. The realistic cause is not an attack but a version bump (`#002` to `#003`) or a typo in a hand-edited file. Closing it means reporting unmatched children inside a list scope, which is a rule this project does not yet have and a policy question -- the template states a minimum, not a whitelist (#19) -- rather than a repair. **Half of it closed in 0.1.2, and it is the half that is not a policy question.** Whether an unmatched element is a defect is the template's business; whether this run looked inside it is this reader's, and it now says: `summary.rulesNotAsked` lists the rules a scope took with it when the walk could not enter it. Reported only where this reader has **already said something is wrong** in that scope: a row matched an element of the wrong kind, or the near-miss lint fired. Every such proposal is then subtracted against the rows some scope did ask, because a list is walked once per item. The first version guessed instead -- any unclaimed element with a truthy `value` -- and was wrong both ways at once: a manufacturer's own property made a conformant file report a rule unasked, against #19's promise, and a two-item list reported 26 unasked with 22 of them run and one printed as an error in the same report. What is still not covered is the middle-segment typo, which draws no near-miss and so has nothing to hang off. **That is now a measured limit rather than an open question.** The proposal was to widen the near-miss lint to structural similarity -- an element one segment off a row's identifier is probably a typo, and a supplier's own identifier is near nothing. It works for an IRI and it cannot work here: **seventeen of the eighteen silenced rows are ECLASS IRDIs, and adjacent ECLASS codes are different real properties.** `0173-1#02-ABH994#003` and `0173-1#02-ABH995#003` are both rows of these tables, and 37 pairs of published IRDIs sit exactly one character apart -- the same distance as the typo. No bound separates them without an ECLASS dictionary this repository deliberately does not carry, and a rule that cannot separate them would bill a supplier for our own uncertainty. The one IRI-shaped row is not enough to build a rule for. Pinned in the suite so that templates whose identifiers are far apart would make this worth revisiting. It draws no finding, moves no exit code, and makes no claim about the file |
| 24 | Three of 02003's ECLASS-CDP supplemental URLs are malformed upstream: two carry a literal space after `.com/` (GeneralInformation, TechnicalPropertyAreas) and one uses `#` where the CDP spelling uses hyphens (ValidDate). | Carried verbatim into the match sets, where they fold to nothing and match nothing. No effect: every one of those elements is matched by its primary semanticId, which is well formed. Recorded as the same kind of fact as #4. |
| 25 | Upstream publishes this template twice in one directory: `IDTA 02035-2_DBP-Part-2_HandoverDocumentation.json` and `IDTA 02035-2_DBP-Part-2_HandoverDocumentation_without_examplevalues.json`. The second name is false. Both files carry the same qualifiers — 22 `SMT/Cardinality`, 12 `ExampleValue`, one `AllowedIdShort` — and the twin differs by exactly two things: it drops the value `"en"` on the single Property under `Language`, and it has no final newline. | The published file is the one vendored, and the SHA-256 recorded beside it is what holds that choice: a filename cannot, because the twin's name describes a file that does not exist. Recorded here so that a later reader reaching for the "without example values" copy — to cross-check the remedy strings, say — learns first that it is not one. |
| 26 | IDTA 02035-2 (Digital Battery Passport, part 2) declares the *same* submodel `semanticId` as 02004 — `0173-1#01-AHF578#003`, a ModelReference with one Submodel key, under the same idShort `HandoverDocumentation` — and asks for something different. Measured between the two vendored files: 22 of 02004's 38 rows are here and none that are not; the primary semanticId is identical on 22 of 22; every match set is a strict *superset* (the additions are all SAMM URNs) and none is narrowed; exactly two rows, `Version` and `Description`, are `ZeroToOne` where 02004 says `One`; and two more, `ClassName` and `ClassId`, carry a different `ExampleValue`. | Two generated tables, not one table with a mask over it. A mask would be invisible to matching — nothing here narrows what 02004 matches — and what a mask cannot do is give one rule id a second remedy sentence, which is exactly where the four differing rows land. The tables sit side by side, each with its own rules: **which** of them answers for a given file is a separate decision, taken where the walk is handed a table. `--profile` takes it: `--profile 02035-2` hands the walk this table, `--profile 02004` the other, and without the flag 02004 answers as it did before. `SMT-D2` reports the choice unless nothing was chosen and nothing was declared — no flag, no mark, the default answering as it always would. An explicit `--profile` is reported even when it names the template that would have answered anyway, because a stored report has to carry what was asked of the run and not only what differed from a default the reader cannot see. (This entry once said the reverse; the code has always done what `smt_d2_the_report_names_the_profile`'s docstring says), because a stored report cannot say which requirements produced it otherwise. What the flag moves is measured: of 02004's 38 generated rows, 22 are asked again — 18 in the same words and 4 in different ones (`ClassId` and `ClassName` carry a different example value, `Version` and `Description` a relaxed cardinality) — and 16 are not asked at all; three of the fourteen hand rules navigate elements this template does not have. The file's own declaration reports and does not choose (#30). This row is what every inherited hand rule cites, because row overlap is what lets one be asked of both tables at all; where a rule is about the VDI classification rather than about overlap, what it stands on is #29 — including the one place this template's own bytes argue with a rule installed for it. |
| 27 | Four of 02035-2's six SubmodelElementList children carry an `idShort` (`Document`, `DocumentClassification`, `DocumentId`, `DocumentVersion`), which AASd-120 forbids; 02004's template leaves those positions unnamed. aas-core3.0 reports 16 verification errors against the published 02035-2 template — those four, plus twelve duplicate-language conceptDescription entries of the kind #6 records for 02004. | Those four rows are labelled with the name the artefact itself carries, so a finding names something a reader can find in the file. `DBP_ITEM_NAMES` gives all six of them our own name anyway, using 02004's word for the same element, so repairing the defect upstream would rename nothing and no divergence would appear where none had. The two positions that obey the metamodel (`LanguageCode`, `DigitalFile`) are named by us, exactly as in 02004. |
| 28 | What distinguishes a 02035-2 file from a 02004 one, measured across the pinned upstream tree. **The submodel supplemental**: 02035-2 declares `urn:samm:io.admin-shell.idta.batterypass.handover_documentation:1.0.0#HandoverDocumentation` in all three of its published artefacts — the JSON, upstream's `_without_examplevalues` twin, and the AASX payload, once each — and no 02004 or 02003 artefact carries it — precision 1.0 over everything published. **`administration.templateId`**: 02035-2's JSON says `https://admin-shell.io/idta-02035-2` but its own AASX says `https://admin-shell.io/idta-02004-2-0`; IDTA 02035-4 says the bare string `IDTA-02003-2-0`; IDTA 02035-3 says `https://admin-shell.io/idta-02023-1-0` where IDTA 02023 itself says `https://admin-shell.io/IDTA 02023-1-0` — the same value in a different case with a different separator. **Neither signal has instance evidence**: of the 14 `kind: Instance` submodels published under `published/`, none carries a supplemental SAMM identifier and only two carry any supplemental at all, each the ECLASS-CDP twin of its own primary id. | The supplemental is the signal and `templateId` is not read: a field whose two published serialisations contradict each other about the same document cannot be evidence, and a suffix match over those values would read 02035-4 as Technical Data. The signal is read in `rules/profiles.py`, outside the matching path, because matching must keep reading the main semanticId alone (#14 and `tests/test_detect.py`). `SMT-D2` reports which template answered, at `info`, which `Report.ok` does not count — the exit code is the answering template's business, not the choosing. What the mark does *not* do is choose: 02004 answers unless `--profile` says otherwise. Precision is measured and recall is not measurable — no published 02035-2 *instance* exists — and a signal like that may say something without taking a check away, which choosing the other table would do 21 checks' worth (#30). |
| 29 | IDTA 02035-2 keeps the VDI 2770 classification, and its own bytes say so three times: `ClassificationSystem` is `SMT/Cardinality = One` as in 02004, its ExampleValue is the same `VDI2770:2020`, and its `ClassId` ExampleValue is `02-04` — one of the twelve classes (#33), with `ClassName` giving that class's English name. (02004's examples are `03-02` and `Operation@en`, which is why those two rows carry different remedy sentences — see #26.) A fourth fact points the other way, and it exists only in this template: the `ClassificationSystem` conceptDescription (`0173-1#02-ABH997#003`) carries the description `The default value "VDI2770:2020" should be used.`, where 02004's carries none. | The hand rules that enforce the classification (D2, D3, D4 and the L5 spelling lint) are installed for this pack as they are for 02004. The evidence is the vendored template rather than the PDF, which is not vendored: a cardinality and two example values, hash-verified, rather than a sentence somebody read. The fourth fact is why L5 stays a `SHOULD` here rather than being raised or dropped: §2.3 names one value as identifying and this template recommends the other, with a modal verb, in bytes this project hash-verifies — two published sentences about one field, and settling them is not this tool's business. A `SHOULD` says which spelling this tool prefers and leaves the exit code alone, so a conformant battery passport written the way its own template recommends draws one warning and still passes. Revisit when a published 02035-2 instance exists to see which sentence its authors followed. |
| 30 | Whether a submodel's own declaration should choose which of the two templates answers. The mark — `urn:samm:io.admin-shell.idta.batterypass.handover_documentation:1.0.0#HandoverDocumentation` in the submodel's supplementals — has **precision 1.0** over everything published upstream: it appears in all three 02035-2 artefacts and in nothing else. Its recall among instances is **0 of 14**: of every `kind: Instance` submodel published under `published/`, none carries a supplemental SAMM identifier at all, only two carry any supplemental, and no 02035-2 instance exists to measure against. Switching on it measured: a file that carries it and is really 02004 loses **21 rule ids** — 18 generated rows and hand rules D6, D8, D9 — and **19 of those flip exit 1 to exit 0** — the 18 generated rows, all `MUST`, and hand rule D8, which is `MUST` where D6 and D9 are `SHOULD`. | The mark reports; `--profile` chooses. Precision is measured and recall is not measurable, and a signal like that may say something without taking a check away. The convenience it would buy is automatic selection for files that carry it, of which none is known to exist; the cost when it is wrong is a validator that says a file passed after declining to check nineteen things about it. `SMT-D2` names the declaration and its remedy names the flag, so an author of a real battery passport is told what to run rather than guessed at. Revisit when a published 02035-2 instance exists to measure recall against. |
| 31 | `xs:date` carries `whiteSpace="collapse" fixed="true"` in the schema W3C publishes for the built-in datatypes, so a conforming processor folds tabs, newlines and runs of spaces and trims the ends *before* the value is matched. aas-core3 does not: its pattern ends with `$`, which in Python matches before a final newline — so it accepts `2025-03-15\n` and refuses `\n2025-03-15`, an asymmetry that is neither reading. | The standard is followed: the value is collapsed, then matched. This is the only place the two readings still differ, and it differs in the safe direction — every value it admits is one XML Schema calls conformant, so refusing it would be a finding against a conformant file, and accepting it where aas-core3 would not leaves the metamodel channel its second opinion. A test in this repository asserted the opposite for a year, on the reasoning that `$` matches before a newline *and therefore* an XML Schema processor refuses the value; the first half is a fact about Python and the second is not a fact about XML Schema. |
| 32 | #26 records that every one of 02035-2's match sets is a strict *superset* of 02004's, the additions all SAMM URNs, and that none is narrowed. The conclusion drawn from that — in `tests/test_dbp_rules.py` and in this project's own reasoning — was that 02035-2 therefore makes nothing stricter, so a file conformant to 02004 is conformant to 02035-2. That does not follow, and it is false. A wider match set is a *tightening* wherever a row has an upper bound: an environment carrying `Documents` under 02004's ECLASS identifier and a sibling under the SAMM URN matches one row under 02004 and two under 02035-2. Measured: `ok=True` with no flag and with `--profile 02004`; `ok=False` with `--profile 02035-2`, `DBP2-E01 the template expects exactly one 'Documents' here; found 2`. | Neither verdict is wrong — each is right for the template that produced it. A file that is a battery passport and carries both spellings of `Documents` really does declare the list twice; a file that is 02004 carries one list and a stranger's element beside it, and this project does not refuse what a template does not mention. What was wrong is the claim that the implication runs one way, so the claim is gone and the counter-example is a test. The practical consequence for a reader: `--profile 02035-2` on a file that is not one can fail it, which is the direction this project treats as worst, and is why the flag is the caller's explicit choice rather than something guessed from the bytes (#28). |
| 33 | The twelve VDI 2770 document classes (`01-01`; `02-01`..`02-04`; `03-01`..`03-06`; `04-01`) are read from **IDTA 02004-2-0 §2.3, Table 1** — which is freely published, is what HD-D3's own `spec` field cites, and is the document this project validates against — cross-checked against the DDC reference implementation's constants. **VDI 2770 Blatt 1:2020-04 itself is sold by DIN Media and was not opened**; an earlier version of this row named it as the source, which was a primary-source claim behind a secondary one. The set cannot be re-derived from bytes this project hash-verifies: neither vendored template carries a `valueList` for `ClassId`, its concept description (`0173-1#02-ABH996#003`) carries a free-text definition only, and no `ABU4xx` IRDI appears anywhere in the vendored data. What the bytes do corroborate is two of the twelve by name: 02035-2 pairs `02-04` with `Certificates, declarations` and 02004 pairs `03-02` with `Operation`. | The set is spelled inline in `rules/handover.py` and HD-D3 refuses anything outside it, at `MUST`. Both edges are asserted: the twelve are each accepted, a value outside them is refused, and the set itself is pinned. **The severity rests on which question the rule asks.** It never reads a classification unless the file has already declared its system to be VDI 2770 Blatt 1:2020 (or the template's own `VDI2770:2020`, #9), so it is not asking "is this one of the classes VDI publishes" but "does the edition this file names define this value" — and a file naming a system that does not define its own class id is self-contradictory rather than merely unidiomatic. That is what separates it from HD-D6, whose vocabulary the vendored bytes themselves introduce with *should*, and which is a `SHOULD`. **The cost, both ways — measured, not argued.** A later VDI 2770 naming more classes names itself differently, and a classification declaring that system is not one this rule reads — asserted. But escaping D3 is not passing: such a file then has no classification in the two accepted spellings, so **HD-D2 fires at MUST and the run exits 1**, and D2's remedy prescribes the 2020 system. That is the template's verdict rather than an accident — the vendored template names this edition — and the honest statement is that a later-edition file fails *as not being this template*, not that it passes. The same gate is exact about the spelling and not about the whitespace: `VDI 2770 Blatt 1:2020-04` names a different edition and draws D2, while the canonical spelling with a space around it does not. It used to. The byte-exact reading was recorded here as deliberate, and the argument against it is two rows away: #34 folds a boolean's case and #31 a date's whitespace, both because a finding on a conformant file is the direction with no second opinion — and #34's rule is a SHOULD while #31's readers -- `HD-D8` and `TD-D1` -- are MUSTs, which makes the case for folding stronger here rather than weaker: this row's rule moves the exit code too, and that is the reason the other two give for folding, not a reason against. The asymmetry was pointing the wrong way. HDL5, which says the spelling is non-canonical, reads the same folded value, so a space cannot turn one finding into the other. The official example's own concept-description prose also mentions a `VDI2770:2018` spelling; it names a different edition's rule and is not accepted (#9 names the two that are). What the MUST is actually exposed to is the twelve being wrong about the edition they claim: a question about where they were read, which is why the provenance above is stated exactly and was corrected once. |
| 34 | `DocumentIsPrimary` is `xs:boolean`, whose lexical space is `true`, `false`, `1`, `0` — case-sensitively, and with `whiteSpace="collapse"` folding the ends. Measured: aas-core3.0 refuses `TRUE` and `" true "` ("Value must be consistent with the value type"), the second of which XML Schema itself would accept after collapsing. | HD-D5 folds case and trims before reading, so both are taken as the author meant them; reading them strictly would report a document as having no primary identifier when it plainly marks one. `yes` is not a boolean in any reading and is not accepted. **An earlier version of this row justified the leniency by saying the metamodel's refusal is "already reported" through the relayed channel. That is the argument this project had already measured and refused** — see #17 and `tests/test_values.py`, where the same backstop turned out to be `META` at `SHOULD`: the screen said so and the exit code did not. It is no truer here. The measurement that does hold: both spellings draw a `META` finding — now asserted, so the second opinion cannot go away silently — and HD-D5 is itself a `SHOULD`, so on this rule the exit code does not move either way and the leniency costs no signal that strictness would have given. Promoting HD-D5 would re-open #17 rather than close this. Same shape as #31, where `xs:date` collapses in the schema and not in aas-core3.0. |
| 35 | The AAS metamodel names BCP 47 for language tags and ships **two** predicates over them, which answer different questions. `matches_bcp_47` is well-formedness — measured, it accepts `en`, `eng`, `enm`, `english`, `abcd` and `abcdefgh` alike, because RFC 5646's primary subtag is `2*3ALPHA / 4ALPHA / 5*8ALPHA` — and it is the only one aas-core3 relays through this project's `meta` channel. `is_bcp_47_for_english` is the other: `^(en|EN)(-.*)?$`, which aas-core3 applies where the metamodel requires something *in English* (AASc-3a-002). IDTA 02004 §2.3 requires an English `ClassName` and does not say which spelling. The choice was written here as delegate-or-copy, and it is not: the tag is folded to lower case and the same predicate is asked, which is neither a reading of our own nor a fork. RFC 5646 §2.1.1 is explicit -- subtags "are to be treated as case insensitive ... there exist conventions for the capitalization of some of the subtags, but these MUST NOT be taken to carry meaning" -- so `En` and `eN` were findings on conformant files, printed under a `saw` line naming the very tag. `eng` stays refused: it is well-formed, it does mean English, and IANA marks `en` as its preferred value, which is a different argument from this one. | HD-D4 calls `is_bcp_47_for_english` — the function, not a copy of its pattern — so what counts as English here is what counts as English in the metamodel, today and after it changes. **This row records a reversal and a reversal back, because the second is the useful part.** `eng` was refused; that was re-read as over-refusal on the grounds that BCP 47's grammar admits a three-letter primary subtag and that a file using `eng` passes aas-core3's verification, so refusing it was this project overruling a layer it delegates. Both premises are true and neither is the question. The grammar is well-formedness, and `english` clears it too — the probe that would have shown the argument proves too much was the one tag not measured. The verification is silent because it never asks whether a *ClassName* is English, not because the metamodel has no view: it has one, it is narrower than the reading that replaced it, and the refused code was character for character that view. Delegation was the right principle applied to the wrong predicate. Calling the function closes the gap that made the mistake possible — a copy agrees on the day it is written and diverges silently afterwards. What stays refused: `eng`, `enm`, `english`, and any tag the metamodel would not call English. **Not** `eN` and `En`: this cell said they were, on the reasoning that the pattern is case-exact and the cost of that is taken as part of the delegation. RFC 5646 §2.1.1 says capitalisation "MUST NOT be taken to carry meaning", the cost fell on conformant files, and the choice was never delegate-or-copy — the tag is folded and the same predicate is asked, which is neither. The left-hand cell was rewritten when that was measured and this one was not, so for a day the record said the opposite of the code and of the test beside it. The remedy names the two spellings. |
| 36 | IDTA publishes two submodel identifiers that more than one template claims — measured from `data/battery-passport/requirements-idta.json`, which indexes twelve template editions; this repository vendors one of those twelve — 02035-2 1.0 — and neither side of the second collision. Its other two vendored templates are editions that index does not hold: 02004 at 2.0.1 where the index has 2.0, and 02003 not at all: `0173-1#01-AHF578#003` (IDTA 02004 and 02035-2) and `https://admin-shell.io/idta/CarbonFootprint/CarbonFootprint/1/0` (IDTA 02023 and 02035-3). This project has generated tables for both sides of the first and neither side of the second. | The first collision is `SMT-D2`'s and stays so: the choice rides on the `Selection` the walk reads, so a verdict cannot move without the sentence that explains it, and #26/#28 record why 02004 answers by default. `BAT-R2` takes only the second, where there is no table to choose between and the report previously said no submodel declared an identifier the tool recognises — the one thing that is not true about such a file. It knows exactly what it is, twice over, and says so; `--profile` settles which was meant and nothing more, because a profile cannot conjure a table. SMT-D1's sentence was corrected in the same change: "recognises" became "has a template table for", which is what it always meant and is now the difference between two findings in one report agreeing. |
| 37 | Nine template elements are `ZeroToOne` — the template permits them to be absent — while the BatteryPass-Ready Data Attribute Longlist v1.3 or the Commission's data-point guidance marks the same attribute mandatory under Regulation (EU) 2023/1542. **Measured, and corrected in 0.1.3: *none* of the nine is required of every battery category the sources name.** One appeared to be. `EnergyRoundTripEfficiencyFade` was carried as unconditional on a single long-list mark, and Annex IV Part A (4) reads "**Where applicable**, energy round trip efficiency and its fade (in %)" -- while the Commission's own data point 58 marks the same attribute `if applicable` for all three categories it names. That reading never reached the table: the join matched attribute names as token sets and the two words in front of the guidance text stopped data point 58 matching the element, so the only source still speaking about it was the spreadsheet. The join now strips a leading qualifier and a leading "pursuant to Article N:" pointer clause before matching (three and six of the seventy-one guidance points respectively, no long-list row either way), and a source reading `conditional` counts as evidence rather than as silence. Remaining capacity is required for light means of transport and *voluntary* for electric vehicles; the capacity threshold for exhaustion inverts — required for EVs, and the guidance marks it `Not to be filled/displayed` for everything else; capacity fade is required by the longlist for all four categories and conditional in the guidance for industrial. | `BAT-R8` reports a row **only when the file settles the category itself**; there is no longer a row it reports of every battery. IDTA 02035-4 makes `BatteryCategory` cardinality `One` and names its vocabulary in the element's own description — `lmt`, `ev`, `industrial`, `stationary` — so a passport that states one has answered the question those rows were waiting on, and the guidance column that value picks is applied as written. Nothing is inferred: only `required` and `required-by-batteries-regulation` draw a finding, so `CapacityThresholdExhaustion` fires for an electric vehicle and stays silent for light means of transport, which is the case this entry was written about — a finding that ignored the category would tell an LMT manufacturer to add a field their own guidance forbids, over-refusal wearing the shape of diligence. Measured on the corpus: a passport declaring `ev` is asked two of the nine and one declaring `lmt` seven; a passport declaring neither is asked none, and the coverage note says so rather than falling silent. **One declaring *two* is asked none as well**, and for a different reason: it has not settled the question, and the two answers demand opposite things. That case was answered by whichever category the walk reached first until 0.1.4 — the same file with the two reversed drew a different set of rows — and the corpus could not see the repair, because every input in it stated exactly one. It now holds one stating both, measured to move from seven warnings and exit 1 to none and exit 0 across that release. The element the front page leads with is `RemainingCapacity` for `lmt`, and it is the one row where all three sources are asserted to agree -- Annex VII Part A (1) carries no qualifier and its part applies to "stationary battery energy storage systems and LMT batteries", guidance data point 62 reads `required` for LMT, and long-list row 60 agrees. That agreement is a test, not a memory: a re-pin that breaks it goes red rather than quietly restoring the shape of the mistake above. **`industrial` and `stationary` are deliberately still unanswered**: they reach only guidance columns named *above 2 kWh*, and a category is not a capacity — the provision that would settle it is Article 77(1), which this repository does not index, and judging on a provision we have not read is what this project refuses. The coverage note counts what was withheld from **this file** rather than what the table holds — a silence a report states is a different thing from one it keeps. **A warning and not an error** for a second reason: two published readings of applicability exist and this pack cannot be told which to answer for, so raising it would fail a file under a reading its author never chose. And "floor": the templates cite no provision, so the join behind the table matched attributes by name — name matching misses every element whose label differs from the prose, and reaches a nested element only when its label happens to match, which is why three of the nine are two collections deep and eight more nested ones may be missing entirely. |
| 38 | The metamodel lets a `Reference` carry more than one key and the published templates do not say which shape any of them must take. Counted across the five official files this repository holds — the 02004 example, the 02003 sample and the three vendored templates — **48 of the 935 references stack more than one key**. Seventeen of those are not `supplementalSemanticIds`: they are `valueId`, `value` and `unitId`, and one in the official 02004 example names the submodel, then the list, then the collection inside it. The remaining thirty-one are supplementals, and this project matches rows on those as well as on the main identifier (`element_candidate_values`, row 14), so the stacked shape is already in a field the matching reads. What has stayed single-keyed is the main `semanticId`: **449 of them across those five files and not one with a second key**. | Values are collected from **every** key of a Reference, and the keys joined by `/` are offered as one more spelling (`semantics.candidate_values`). Reading only `keys[0]` would refuse a conformant instance that stacks its keys, and refusing a conformant file costs more than missing a defect. The branch is exercised by published material — judging the official example calls the collector 1,200 times and 23 of those are multi-key — but no verdict has yet turned on it: cut the collector back to the first key and the example's report comes back byte for byte the same. What goes red is `tests/test_detect.py`, which constructs the case the published files have not produced. So this is a guard against a shape the material shows is legal and has not yet used against us. |
| 39 | Five Handover rows share a `semanticId` with the row directly above them: a `SubmodelElementList` and its own item are given the same identifier by the template — `DigitalFiles`/`DigitalFile` are both `0173-1#02-ABK126#002`, and so are `Language`/`LanguageCode`, `RefersToEntities`/`RefersTo`, `BasedOnReferences`/`BasedOn`, `TranslationOfEntities`/`TranslationOf`. Technical Data has none. The battery part, being a second Handover template over the same elements, has two of the five — `Language`/`LanguageCode` and `DigitalFiles`/`DigitalFile`; this entry said it had none until they were counted. | Nothing is reported: an identifier a template reuses for a container and its content is the template's business, and the walk tells them apart by where they sit. Where it matters is navigation by label. `child_of` matches on the identifier, so inside a collection holding one of these lists the two labels are **interchangeable** — asking for the item returns the list, and the label looks like it is doing work it is not. No rule asks for the item today; a test asserts that every `child_of` a rule makes comes back as the kind its row names, so the day one does the suite says so rather than the rule quietly iterating a list as if it were a file. It also decides a remedy: an element written as the item alone matches the *list's* row, and being told to change it into a list would empty it, so the finding says to wrap it instead. |
| 40 | A `Property` the template marks mandatory can be present and carry no value, and nothing said so. Cardinality is the generated rules' question and content is the hand rules', and every hand rule reads a value through `property_value`, which returns `None` when it is absent — correctly, because an absent value is not a content defect for them to report. So the two questions met and neither covered this: **measured, a Technical Data file with the `value` key deleted from all nine of its properties drew no finding at all and left by 0**, and a Handover file with all eleven deleted drew one. The metamodel does not object either: `Property.value` is optional, so a valueless property is well formed, and the relayed channel says nothing. | Reported at the generated row's own severity, which is `MUST`. The count is satisfied in form and the requirement is not, and it is this tool's own promise that goes false: "the template requires this element" answered yes about an element holding nothing, which is the shape of pass this project treats as worst. The remedy is the row's own rather than the count's — the count's advice is "provide one", which here tells a reader to add a second empty element. **Raised to MUST only after measuring what the standards body publishes**: across IDTA's own 02004 example and the three 02003 samples, all 102 properties carry a value and none is absent, so the rule costs nothing on published material. It reaches 10 rows in 02004, 7 in 02003 and 5 in 02035-2. **`value: ""` is deliberately not this rule's business.** The empty string *is* a value of type `xs:string`, and calling it nothing is a reading about content where this rule is about presence — the metamodel draws the same line, and so does the file: one shape has the key and one does not. Where an empty value is wrong for a typed property the relayed channel already says so ("Value must be consistent with the value type"), and where it is wrong for a dated one `TD-D1` does. What is left unjudged is an empty `xs:string`, which is recorded here rather than reported, because a rule that cannot say why an empty name is worse than a short one is a rule inventing a requirement. |
| 41 | `HD-D9` reports a document/entity reference that resolves to nothing, and its citation quoted IDTA 02004-2-0 §2.2 as "the creation of an Entity element is required" — which reads as an unconditional requirement and is not one. The paragraph it comes from (page 7) opens "the documentation of a complex piece of equipment **may** include further supplier parts. These parts **can** be marked as separate entities", then describes two categorisations, self-managed and co-managed, and closes "In any case, the creation of an Entity element is required" — *in either of those two cases*, not in every file. The sentence that does bind is conditional too: "If a 'Document' relates to a dependent … Entity and not to the Asset of the AAS itself, a ReferenceElement called 'DocumentedEntity' **shall** be used." §2.8, which covers the other three labels this rule walks, describes the DocumentVersion reference lists ("Forms a generic refers to-relationship…") and requires nothing of what they point at. | The rule stays **SHOULD**, and the citation now carries the condition. A proposal to split it — `MUST` for `DocumentedEntity` because §2.2 says "required", a lint for the three §2.8 labels — was **refuted by the original text**: it would promote a conditional clause to an unconditional requirement, which is the defect the front page carried until 0.1.3 and the one `SOFT_QUALIFIER` exists to catch elsewhere. Nothing in either clause makes *resolution* a requirement; what §2.2 requires is that an Entity be created where a file marks supplier parts as entities at all, and the generated cardinality row already asks whether the `DocumentedEntity` element is present. Read from the published document rather than from memory: IDTA 02004-2-0 *Submodel Handover Documentation* 2.0, sha256 `772a94bca7974e03b47920d127777033a99db6d05dc8a576815cbb8ff8f43131`, page 7. |
| 42 | Four hand rules navigate a wrapping list as `children_of(child_of(parent, "<Label>s", tables) or parent, "<Label>", tables)`. Each of those lists is `SMT/Cardinality: One`, so a file that states its `DocumentId`s, classifications or digital files directly on their parent is not conformant and a generated rule refuses it — measured, `HD-E03` at `error`, exit 1. What the `or parent` half decides is not whether such a file is accepted but whether the hand rules also read what it did state. **And the four lines, written identically, do not behave identically.** For `DocumentIds` and `DocumentClassifications` the template gives the list and its child *different* identifiers, so the guard misses, the fallback runs, and `HD-D5`, `HDL4` and `HD-D4` are all reported against a flat file — measured. For `DigitalFiles` it gives the list and its sole child **one** identifier, `0173-1#02-ABK126#002` — which is #39, one of the five places this template does that — and matching does not consult element kind once an identifier hits (`_matches_row`), so `child_of(version, "DigitalFiles")` returns the flat `File` itself. The guard is truthy, `or version` never runs, the rule asks a `File` for children it does not have, and `HD-D7` and `HD-D10` say nothing. | The fallback is kept and now gated for the three rows it reaches. It cannot cost a conformant file anything — a conformant file has the list, `child_of` finds it, and the second half is never reached — and matching by semanticId is what makes the reading available at all: a collection wearing `DocumentId`'s identifier is a `DocumentId` wherever a file puts it. It is the same reading the container chain was given when it stopped at the first payload it could not read: a reader that reports one defect sends an author away to fix it and then names the next one of the same kind on their return. **#39 records the shared identifier and the direction already guarded**: asking for the *item* and getting the *list*, which `tests/test_engine_seam.py` catches by spying on every `child_of` a rule makes and refusing any answer whose kind is not the one its row names. That gate runs on files that have the list, because a conformant file does. What is new here is the same interchangeability in the **other** direction — asking for the *list* and getting the *item* — which only a file missing the list can produce, and which therefore sits outside what that spy can see. The fourth row is **recorded as a limitation rather than repaired**, and the limitation is pinned by a test that asserts the mechanism rather than the symptom, so a change in matching cannot leave this row quietly false. Repairing it is not free in either available direction. Asking for the children directly when the list lookup comes back empty reports `HD-D10` against the *list element* on a version carrying an empty `DigitalFiles`, since the list wears the child's identifier too — trading a finding not made for a finding with the wrong subject. Teaching `child_of` to require a list row's element kind is a change to matching, which is the walk's business and not one rule's (#11), and would want its own measurement against the corpus before it moved. What is lost meanwhile is bounded: a finding not made about a file that is already failing. |
